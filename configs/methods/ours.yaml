method:
  name: ours
  params:
    # Rank-Gate sensitivity, collected once per real optimizer step.
    sensitivity_beta: 0.95
    sensitivity_warmup_steps: 1
    sensitivity_clip_max: 10.0
    gamma: 1.0
    delta_max: 10.0
    history_beta: 0.95
    history_lambda: 1.0
    base_precision: 1.0
    alpha_max: 0.5
    eps: 0.000000000001
    min_update_energy: 0.0000000000000001
federation:
  mode: asynchronous
